# This is an example of the kind of things you can do in a configuration file.
# All flags used by the client can be configured here. Run Certbot with
# "--help" to learn more about the available options.

# Use a 4096 bit RSA key instead of 2048
rsa-key-size = {{ letsencrypt_rsa_key_size }}

# Uncomment and update to register with the specified e-mail address
email = {{ letsencrypt_email }}

# Uncomment and update to generate certificates for the specified
# domains.
domains ={% for item in letsencrypt_certs | list %} {{ letsencrypt_certs[item].url }} {% endfor %}

# Uncomment to use a text interface instead of ncurses
# text = True

# Uncomment to use the standalone authenticator on port 443
# authenticator = standalone
# standalone-supported-challenges = tls-sni-01

# Uncomment to use the webroot authenticator. Replace webroot-path with the
# path to the public_html / webroot folder being served by your web server.
{% if letsencrypt_single_webroot != "none" %}
authenticator = webroot
webroot-path = "{{ letsencrypt_single_webroot }}"
{% else %}
# authenticator = webroot
# webroot-path = /usr/share/nginx/html
{% endif %}

{{ letsencrypt_extra_conf }}